<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="out.example.dietsupport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExerciseInfoActivity"></activity>
        <activity
            android:name=".dialogs.ScheduleRemoveActivity"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />
        <activity
            android:name=".dialogs.ScheduleAddActivity"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />
        <activity android:name=".dialogs.FoodAdminActivity" />
        <activity
            android:name=".dialogs.AddFoodActivity"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".BMIActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>